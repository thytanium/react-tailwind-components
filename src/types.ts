export enum Variant {
  PRIMARY,
  LIGHT,
  DARK,
  SUCCESS,
  WARNING,
  DANGER,
}

export enum TextColor {
  WHITE,
  LIGHT,
  DARK,
  BLACK,
}

export enum FontWeight {
  HAIRLINE,
  THIN,
  LIGHT,
  NORMAL,
  MEDIUM,
  SEMIBOLD,
  BOLD,
  EXTRABOLD,
  BLACK,
}

export type ZIndex = 0 | 10 | 20 | 30 | 40 | 50;

export enum Axis {
  X,
  Y,
  BOTH,
}

export enum Flex {
  CENTER,
}
