import * as React from 'react';
import { Modal, ModalBody } from '../..';

export default function StackShowcase(): React.ReactElement {
  return (
    <div>
      {/* <Modal>
        <ModalBody>
          <p>Never ever ever ever times infinity</p>
        </ModalBody>
      </Modal> */}
    </div>
  );
}
